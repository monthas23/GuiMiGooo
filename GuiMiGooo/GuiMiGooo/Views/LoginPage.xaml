<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"             
             xmlns:local="clr-namespace:GuiMiGooo.ViewModels"            
             x:Class="GuiMiGooo.Views.LoginPage">


    <ContentPage.BindingContext>
        <local:LoginViewModel/>
    </ContentPage.BindingContext>

    <StackLayout Orientation="Vertical">

        <Entry Text="{Binding Email}"
                   Placeholder="Ur Email"/>
        <Entry Text="{Binding Password}"
                   Placeholder="Ur Password"
                   IsPassword ="true"/>

        <Entry Text="{Binding Username}"
                   Placeholder="Confirm ur password here"
                   IsPassword ="true"/>


        <Entry Text="{Binding PhoneNumber}"
                   Placeholder="Ur Phone Number"
                   IsPassword ="False"/>


        <Entry Text="{Binding CompagnyCode}"
                   Placeholder="MTN o Orange"
                   IsPassword ="False"/>

        <Button Command="{Binding RegistrationComnand}"
                    Text="Register"/>



        <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
    </StackLayout>
</ContentPage>